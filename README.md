# Banca-proyecto-final

Repositorio monorepo para los componentes del sistema bancario del taller.

- Rama `config-data`: contiene el repositorio de configuraciones para Spring Cloud Config.
- Rama `config-service`: contiene el microservicio Config Server.
